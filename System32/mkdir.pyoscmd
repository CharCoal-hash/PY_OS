import os

def mkdir(path):
    try:
        os.makedirs(path, exist_ok=True)
        print(f"Directory {path} created successfully.")
    except Exception as e:
        print(f"Error creating directory: {e}")

# Call the function with the path
mkdir("Z:\\PY_OS\\NewFolder")
